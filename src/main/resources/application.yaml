spring:
  application:
    name: library
  datasource:
    url: jdbc:mysql://localhost:3306/demo_db
    username: root
    password: example
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show_sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql

